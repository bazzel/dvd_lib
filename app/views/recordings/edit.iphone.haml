%div
  .toolbar
    %h1= @recording.name
    = link_to('Back', '#', :class => 'back')
  .autosubmitting
    - form_for @recording do |f|
      %ul.edit
        %li
          - unless @recording.photo_url.blank?
            .photo= image_tag(@recording.photo_url, :height => "148")
          .name= @recording.name
          .genres= @recording.genres(&:name).join(', ')
          .description= @recording.description
        %li 
          Seen
          %span.toggle= f.check_box :seen