- title 'New genre'

.block
  .secondary-navigation
    %ul
      %li.first= link_to 'List', genres_path
      %li.active= link_to 'New', new_genre_path
    .clear
  .content
    %h2.title= yield(:title)
    .inner
      = render :partial => 'form'
  
/ - content_for :sidebar, render(:partial => 'sidebar')