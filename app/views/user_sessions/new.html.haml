- semantic_form_for @user_session, :url => user_session_path do |f|
  - f.inputs do
    = f.input :username
    = f.input :password
    = f.input :remember_me, :as => :boolean

  - f.buttons do
    = button_tag("Login")