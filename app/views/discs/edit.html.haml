- title "Edit Disc"

.block
  .secondary-navigation
    %ul
      %li.first= link_to 'List', discs_path
      / %li= link_to 'New', new_disc_path
      %li.active= link_to "Edit", edit_disc_path
    .clear
  .content
    %h2.title= yield(:title)
    .inner
      = render :partial => 'form'
  
/ - content_for :sidebar, render(:partial => 'sidebar')